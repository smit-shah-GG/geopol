---
phase: 20-daemon-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - frontend/src/admin/panels/ProcessTable.ts
  - frontend/src/admin/admin-styles.css
autonomous: false

must_haves:
  truths:
    - "Admin process table shows paused state per job with visual indicator"
    - "Admin process table shows consecutive failure count with warning color"
    - "Pause/resume buttons toggle job state via admin API"
    - "Trigger button works for all daemon types (no more 501 errors)"
    - "Human verified: admin dashboard shows live APScheduler job state"
  artifacts:
    - path: "frontend/src/admin/panels/ProcessTable.ts"
      provides: "Updated process table with pause/resume buttons and extended fields"
      contains: "pause"
    - path: "frontend/src/admin/admin-styles.css"
      provides: "Styles for paused state, failure warnings"
      contains: "paused"
  key_links:
    - from: "frontend/src/admin/panels/ProcessTable.ts"
      to: "/api/v1/admin/processes/{daemon_type}/pause"
      via: "AdminClient.post()"
      pattern: "pause"
    - from: "frontend/src/admin/panels/ProcessTable.ts"
      to: "/api/v1/admin/processes"
      via: "AdminClient.get() polling"
      pattern: "processes"
---

<objective>
Update the admin dashboard ProcessTable to display APScheduler's extended job state (paused indicator, consecutive failures, last error, last duration) and add pause/resume toggle buttons alongside the existing trigger button. Verify end-to-end with human checkpoint.

Purpose: The backend now returns rich scheduler state -- the frontend must surface it so operators can see and control all background jobs from the admin dashboard.
Output: Updated ProcessTable.ts with pause/resume controls and extended job info display.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-daemon-consolidation/20-RESEARCH.md
@.planning/phases/20-daemon-consolidation/20-CONTEXT.md
@.planning/phases/20-daemon-consolidation/20-02-SUMMARY.md

@frontend/src/admin/panels/ProcessTable.ts
@frontend/src/admin/admin-styles.css
@frontend/src/admin/admin-layout.ts
@src/api/schemas/admin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ProcessTable with pause/resume controls and extended job state</name>
  <files>
    frontend/src/admin/panels/ProcessTable.ts
    frontend/src/admin/admin-styles.css
  </files>
  <action>
  Read the existing ProcessTable.ts to understand the current render pattern, then modify:

  1. **Update the process row rendering** to display new fields from the extended ProcessInfo:
     - `paused`: Show a "PAUSED" badge with distinct color (amber/yellow) when true. Override the status dot.
     - `consecutive_failures`: Show as a small badge next to status. If > 0, use warning color (orange). If >= 5 (auto-pause threshold), use error color (red).
     - `last_error`: Show as a truncated tooltip or expandable detail row. On hover or click, show full error message.
     - `last_duration`: Show formatted duration (e.g., "2.3s", "1m 45s") in a new column or below the status line.

  2. **Add pause/resume toggle button** per daemon row:
     - If job is NOT paused: show a "Pause" button (muted style)
     - If job IS paused: show a "Resume" button (primary/green style)
     - On click: POST to `/api/v1/admin/processes/{daemon_type}/pause` or `/resume`
     - Show toast on success ("Job paused" / "Job resumed")
     - Show toast on error
     - Refresh the table after action

  3. **Fix trigger button:** The 501 error path is now gone. The trigger button should work for ALL daemon types. Remove any special-case handling for 501 responses if present. On success, show toast "Job triggered: {name}".

  4. **Layout adjustment:** The action column now has 2 buttons (Trigger + Pause/Resume). Ensure they fit -- use a button group or stack vertically if needed.

  5. **CSS additions** in admin-styles.css:
     - `.process-paused` -- amber/yellow background tint for paused row
     - `.badge-failures` -- small inline badge with failure count
     - `.badge-failures.warning` -- orange for 1-4 failures
     - `.badge-failures.critical` -- red for 5+ failures
     - `.btn-pause`, `.btn-resume` -- styled action buttons
     - `.process-duration` -- muted small text for last duration
     - `.process-error` -- truncated error text with hover tooltip
  </action>
  <verify>
    `grep "pause" frontend/src/admin/panels/ProcessTable.ts` returns matches.
    `grep "consecutive_failures\|last_error\|last_duration\|paused" frontend/src/admin/panels/ProcessTable.ts` returns matches for all 4 new fields.
    `grep "process-paused\|badge-failures\|btn-pause" frontend/src/admin/admin-styles.css` returns matches.
  </verify>
  <done>
    ProcessTable displays all extended job state fields. Pause/resume toggle buttons work. Trigger button works for all daemon types. Failure count shown with warning/critical coloring. Paused rows visually distinct.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete APScheduler daemon consolidation:
    - All 9 background jobs running under APScheduler in FastAPI process
    - Admin dashboard shows live job state with pause/resume/trigger controls
    - Heavy jobs isolated in ProcessPoolExecutor with mutual exclusion
    - Graceful shutdown with 30s timeout
    - Failure tracking with auto-pause after 5 consecutive failures
  </what-built>
  <how-to-verify>
    1. Start the server: `uv run uvicorn src.api.app:create_app --factory --port 8000`
    2. Check server logs for "APScheduler started with 9 jobs" (or similar)
    3. Verify GET /api/v1/admin/jobs returns all 9 registered jobs:
       `curl -H "X-Admin-Key: $ADMIN_KEY" http://localhost:8000/api/v1/admin/jobs | python -m json.tool`
    4. Open admin dashboard at http://localhost:5173/admin
    5. Navigate to Processes panel -- verify all daemon types show "scheduled" status with next_run times
    6. Click "Pause" on GDELT Poller -- verify row shows "PAUSED" badge and status changes
    7. Click "Resume" on GDELT Poller -- verify status returns to "scheduled"
    8. Click "Trigger" on Advisory Poller -- verify toast says "Job triggered" (not 501 error)
    9. Check server logs for the triggered advisory poll cycle executing
    10. Verify no Polymarket asyncio.create_task in logs (old loop removed)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- ProcessTable renders pause/resume buttons for every daemon row
- No 501 errors on trigger for any daemon type
- Paused jobs show visual indicator
- Consecutive failure count displayed with color coding
- Admin dashboard reflects live APScheduler state
</verification>

<success_criteria>
- Admin process table shows extended APScheduler fields
- Pause/resume/trigger all functional from admin UI
- Human verification confirms end-to-end daemon consolidation working
- All DAEMON-01 through DAEMON-05 requirements verified
</success_criteria>

<output>
After completion, create `.planning/phases/20-daemon-consolidation/20-03-SUMMARY.md`
</output>
