---
phase: 24-global-seeding-globe-layers
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - src/ingest/gdelt_poller.py
  - src/ingest/advisory_poller.py
  - src/database/event_storage.py
autonomous: true

must_haves:
  truths:
    - "New GDELT events are stored with ISO alpha-2 country codes (not FIPS)"
    - "New GDELT events include lat/lon coordinates when available"
    - "Advisory poll writes advisories to PostgreSQL travel_advisories table"
    - "Existing 61K events with FIPS country codes are retroactively converted to ISO"
  artifacts:
    - path: "src/ingest/gdelt_poller.py"
      provides: "FIPS-to-ISO conversion at ingestion + lat/lon extraction"
      contains: "fips_to_iso"
    - path: "src/ingest/advisory_poller.py"
      provides: "Advisory persistence to PostgreSQL alongside in-memory store"
      contains: "travel_advisories"
  key_links:
    - from: "src/ingest/gdelt_poller.py"
      to: "src/seeding/fips.py"
      via: "import fips_to_iso for country code conversion"
      pattern: "from src\\.seeding\\.fips import"
    - from: "src/ingest/advisory_poller.py"
      to: "src/db/models.py"
      via: "TravelAdvisory ORM model for DB persistence"
      pattern: "TravelAdvisory"
---

<objective>
Fix GDELT event ingestion to convert FIPS codes to ISO at ingestion time and extract lat/lon coordinates. Persist travel advisories to PostgreSQL for cross-process access by the baseline risk heavy job.

Purpose: Without FIPS-to-ISO conversion, 142 of 253 country codes map to the wrong country (e.g., Israel shows up as Iceland). Without advisory persistence, the baseline risk heavy job (subprocess) cannot access advisory data. Without lat/lon, the heatmap layer has no event coordinates.

Output: Modified GDELT poller, modified advisory poller, retroactive FIPS-to-ISO UPDATE on existing events.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-global-seeding-globe-layers/24-CONTEXT.md
@.planning/phases/24-global-seeding-globe-layers/24-RESEARCH.md
@.planning/phases/24-global-seeding-globe-layers/24-01-SUMMARY.md
@.planning/phases/24-global-seeding-globe-layers/24-02-SUMMARY.md
@src/ingest/gdelt_poller.py
@src/ingest/advisory_poller.py
@src/ingest/advisory_store.py
@src/database/event_storage.py
@src/database/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify GDELT poller for FIPS conversion + lat/lon extraction</name>
  <files>
    src/ingest/gdelt_poller.py
    src/database/event_storage.py
  </files>
  <action>
1. In `src/ingest/gdelt_poller.py`, modify `_gdelt_row_to_event()`:
   - Import `from src.seeding.fips import fips_to_iso` at the top of the file
   - After extracting `country_iso` from `ActionGeo_CountryCode` / `Actor1CountryCode`, convert it: `country_iso = fips_to_iso(country_iso) if country_iso else None`
   - Extract lat/lon: parse `ActionGeo_Lat` and `ActionGeo_Long` from the GDELT CSV row. These are float columns. Use the same `pd.notna()` guard pattern as other numeric fields.
   - Pass `lat=lat, lon=lon` to the Event constructor

2. In `src/database/event_storage.py`:
   - Find the INSERT statement for events. Add `lat` and `lon` to the column list and parameter bindings.
   - Add a one-time migration method or startup check: if the `lat` column doesn't exist on the events table, run `ALTER TABLE events ADD COLUMN lat REAL; ALTER TABLE events ADD COLUMN lon REAL;` to handle existing databases. Use a try/except around each ALTER TABLE (column might already exist). This is SQLite -- no transactional DDL concerns.
   - Add a one-time FIPS-to-ISO retroactive conversion method: `def migrate_fips_to_iso(self) -> int` that:
     a. Imports FIPS_TO_ISO from src.seeding.fips
     b. For each (fips_code, iso_code) pair in FIPS_TO_ISO where fips != iso:
        Runs `UPDATE events SET country_iso = ? WHERE country_iso = ?` with (iso_code, fips_code)
     c. Also handles 3-letter ISO alpha-3 codes (64 unique in DB): use pycountry to convert
     d. Returns count of updated rows
     e. This should be idempotent -- running it twice has no effect (already-converted rows won't match FIPS codes)
   - Call `migrate_fips_to_iso()` at the end of `__init__()` or in a `ensure_schema()` method. Log the result. This runs once per startup and is fast (simple UPDATE WHERE).

NOTE: Do NOT generate any new static dicts of country codes. Use only the imported FIPS_TO_ISO dict (loaded from CSV) and pycountry library.
  </action>
  <verify>
    - Read the modified `_gdelt_row_to_event()` and verify `fips_to_iso()` is called on country_iso
    - Read the modified `_gdelt_row_to_event()` and verify `ActionGeo_Lat` and `ActionGeo_Long` are extracted
    - `grep "fips_to_iso" src/ingest/gdelt_poller.py` finds the import and usage
    - `grep "lat" src/database/event_storage.py` finds lat column handling
    - `grep "migrate_fips_to_iso" src/database/event_storage.py` finds the retroactive conversion method
  </verify>
  <done>GDELT poller converts FIPS to ISO at ingestion time, extracts lat/lon coordinates, and event_storage handles lat/lon columns + retroactive FIPS conversion.</done>
</task>

<task type="auto">
  <name>Task 2: Add advisory persistence to PostgreSQL</name>
  <files>src/ingest/advisory_poller.py</files>
  <action>
1. In `src/ingest/advisory_poller.py`, modify the `_poll_once()` method (or equivalent method that calls `AdvisoryStore.update(all_advisories)`):
   - AFTER the existing `AdvisoryStore.update(all_advisories)` call, add a DB persistence step
   - Import `from src.db.models import TravelAdvisory` and `from src.db.postgres import async_session_factory, init_db`
   - For each advisory dict in `all_advisories`, extract:
     - `country_iso`: from the dict's `country_iso` key
     - `source`: from the dict (should be 'us_state_dept' or 'uk_fcdo')
     - `level`: advisory level (1-4)
   - UPSERT to `travel_advisories` table: for each (country_iso, source) pair, either insert or update the existing row. Use SQLAlchemy's `insert().on_conflict_do_update()` with the unique constraint on (country_iso, source).
   - Wrap the entire DB write in a try/except -- advisory persistence is non-critical. If DB write fails, log warning and continue (in-memory store still works for API).
   - Keep the existing `AdvisoryStore.update()` call unchanged -- it still serves the advisory API endpoint.

2. The advisory dict structure (from the existing code) should contain at minimum:
   - `country_iso`: 2-letter ISO code
   - `source`: string identifying the advisory source
   - `level`: integer 1-4

   Read the existing `_poll_once()` method to understand the exact dict structure and extract the correct fields.

NOTE: Do NOT modify or regenerate the existing COUNTRY_NAME_TO_ISO dict (lines 44-271). It already passed content filters. Leave it as-is.
  </action>
  <verify>
    - `grep "TravelAdvisory" src/ingest/advisory_poller.py` finds the import
    - `grep "travel_advisories" src/ingest/advisory_poller.py` or `grep "on_conflict" src/ingest/advisory_poller.py` finds the UPSERT logic
    - The existing `AdvisoryStore.update()` call is still present (in-memory path preserved)
  </verify>
  <done>Advisory poller writes to both in-memory AdvisoryStore AND PostgreSQL travel_advisories table. DB write is non-critical (try/except). Existing COUNTRY_NAME_TO_ISO dict untouched.</done>
</task>

</tasks>

<verification>
- GDELT poller imports fips_to_iso and applies it to country codes
- GDELT poller extracts ActionGeo_Lat/Long from CSV rows
- Event dataclass lat/lon fields are populated in Event constructor call
- event_storage.py handles lat/lon in INSERT and has retroactive FIPS migration
- Advisory poller persists to travel_advisories table via UPSERT
- No new static country-code dicts generated
- No modification to existing COUNTRY_NAME_TO_ISO dict
</verification>

<success_criteria>
New GDELT events arrive with ISO country codes (not FIPS) and lat/lon coordinates. Advisory data is persisted to PostgreSQL for cross-process access. Existing events have been retroactively converted from FIPS to ISO codes.
</success_criteria>

<output>
After completion, create `.planning/phases/24-global-seeding-globe-layers/24-03-SUMMARY.md`
</output>
