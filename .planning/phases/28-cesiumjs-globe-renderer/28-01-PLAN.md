---
phase: 28-cesiumjs-globe-renderer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/src/components/NavBar.ts
autonomous: true

must_haves:
  truths:
    - "CesiumJS is installed and importable in the frontend project"
    - "Old rendering packages (globe.gl, three, deck.gl, maplibre-gl) are removed from package.json"
    - "Vite dev server starts without errors and CESIUM_BASE_URL is defined"
    - "NavBar shows a 3-pill segmented control [3D] [CV] [2D] on the /globe route"
  artifacts:
    - path: "frontend/package.json"
      provides: "cesium + vite-plugin-static-copy dependencies; old deps removed"
      contains: "cesium"
    - path: "frontend/vite.config.ts"
      provides: "CesiumJS Vite integration (static-copy, CESIUM_BASE_URL, cesium manual chunk)"
      contains: "viteStaticCopy"
    - path: "frontend/src/components/NavBar.ts"
      provides: "3-pill segmented control dispatching globe-view-toggle with mode payload"
      contains: "nav-scene-mode"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "node_modules/cesium/Build/Cesium"
      via: "viteStaticCopy targets"
      pattern: "cesiumSource.*Workers"
    - from: "frontend/src/components/NavBar.ts"
      to: "CesiumMap (Plan 02)"
      via: "globe-view-toggle CustomEvent with { mode }"
      pattern: "globe-view-toggle.*mode"
---

<objective>
Set up CesiumJS build infrastructure and update NavBar for 3-mode scene control.

Purpose: Establish the build environment (npm deps, Vite config) so that Plan 02 can import from `cesium` and build successfully. Update NavBar to dispatch the new 3-mode toggle event. Remove old rendering dependencies that are no longer needed.

Output: Working Vite dev server with CesiumJS available, old deps purged, NavBar ready for 3-mode operation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-cesiumjs-globe-renderer/28-CONTEXT.md
@.planning/phases/28-cesiumjs-globe-renderer/28-RESEARCH.md
@frontend/package.json
@frontend/vite.config.ts
@frontend/src/components/NavBar.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap npm dependencies and configure Vite for CesiumJS</name>
  <files>frontend/package.json, frontend/vite.config.ts</files>
  <action>
1. In `frontend/`, run npm uninstall for all 10 old packages:
   ```
   npm uninstall globe.gl three @types/three @deck.gl/mapbox @deck.gl/core @deck.gl/layers @deck.gl/geo-layers @deck.gl/aggregation-layers deck.gl maplibre-gl
   ```

2. Install CesiumJS and the static-copy plugin:
   ```
   npm install cesium@^1.139.0
   npm install -D vite-plugin-static-copy@^3.2.0
   ```

3. Rewrite `frontend/vite.config.ts`:
   - Import `viteStaticCopy` from `vite-plugin-static-copy`
   - Define `cesiumSource = 'node_modules/cesium/Build/Cesium'` and `cesiumBaseUrl = 'cesiumStatic'`
   - Add `define: { CESIUM_BASE_URL: JSON.stringify('/' + cesiumBaseUrl) }` to the config
   - Add `viteStaticCopy` plugin with 4 targets: ThirdParty, Workers, Assets, Widgets (all `dest: cesiumBaseUrl`)
   - Replace `manualChunks` -- remove `deckgl`, `maplibre`, `globe` chunks; add `cesium: ['cesium']`; keep `d3: ['d3']`
   - Keep existing `resolve.alias`, `build.target`, `build.sourcemap`, `server.proxy` unchanged

IMPORTANT: Follow the exact Vite config pattern from 28-RESEARCH.md Pattern "Vite Config (Official Cesium Pattern)". The `cesiumSource` path must point to `node_modules/cesium/Build/Cesium` (capital C).

Do NOT use `vite-plugin-cesium` -- it is abandoned.
  </action>
  <verify>
Run `cd frontend && npx vite build --mode development 2>&1 | head -30` to verify the build starts without import errors. The build may warn about missing CesiumMap.ts (not yet created) but should NOT error on cesium imports or missing CESIUM_BASE_URL.

Also verify: `grep -c "globe.gl\|three\|maplibre-gl\|deck.gl" frontend/package.json` returns 0 (all old deps removed).
  </verify>
  <done>
package.json has cesium + vite-plugin-static-copy; zero references to globe.gl, three, deck.gl, maplibre-gl. vite.config.ts has CESIUM_BASE_URL define, viteStaticCopy plugin with 4 Cesium targets, cesium manual chunk.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace NavBar toggle with 3-pill segmented control</name>
  <files>frontend/src/components/NavBar.ts</files>
  <action>
Replace the single `<button class="nav-view-toggle">` with a 3-pill segmented control:

1. Change `STORAGE_KEY` value remains `'geopol-globe-mode'`. Stored values change from `'3d'|'2d'` to `'3d'|'columbus'|'2d'`.

2. Replace the `viewToggle` button with a container div and 3 pill buttons:
   ```typescript
   const sceneModeContainer = h('div', { className: 'nav-scene-mode' });
   const modes: Array<{ id: '3d' | 'columbus' | '2d'; label: string }> = [
     { id: '3d', label: '3D' },
     { id: 'columbus', label: 'CV' },
     { id: '2d', label: '2D' },
   ];
   ```
   - Read initial mode from localStorage (default `'3d'`)
   - For each mode, create a `<button>` with class `scene-pill`, add `active` class if it matches current mode
   - On click, dispatch `globe-view-toggle` CustomEvent with `{ detail: { mode: pill.id } }`
   - Store pill buttons in an array for updating active state

3. Listen for `globe-mode-changed` to sync:
   ```typescript
   const modeChangedHandler = ((e: CustomEvent<{ mode: string }>) => {
     for (const btn of pillButtons) {
       btn.classList.toggle('active', btn.dataset['mode'] === e.detail.mode);
     }
   }) as EventListener;
   ```

4. Show/hide the segmented control based on route (same as current toggle): `sceneModeContainer.style.display = current === '/globe' ? 'inline-flex' : 'none'`

5. Place `sceneModeContainer` inside `rightContainer` (replacing `viewToggle`).

6. Add appropriate `aria-label` attributes: container gets `'Globe scene mode'`, each pill gets `'Switch to 3D/Columbus View/2D mode'`.

CSS: Add styles for the segmented control in a `<style>` block or inline. The pills should be compact:
- `.nav-scene-mode`: `display: inline-flex; gap: 2px; background: rgba(255,255,255,0.08); border-radius: 6px; padding: 2px;`
- `.scene-pill`: `padding: 4px 10px; border: none; background: transparent; color: var(--text-muted); border-radius: 4px; cursor: pointer; font-size: 12px; font-weight: 600;`
- `.scene-pill.active`: `background: var(--accent, #4080dd); color: #fff;`

Inject these styles by appending a `<style>` element to `document.head` at module scope (same pattern used elsewhere in the codebase, e.g., admin styles).
  </action>
  <verify>
TypeScript compilation: `cd frontend && npx tsc --noEmit 2>&1 | grep -i "NavBar\|error" | head -10`

Note: The build will have errors from globe-screen.ts and DeckGLMap.ts importing deleted packages -- this is expected and resolved in Plan 03. NavBar.ts itself must compile cleanly.
  </verify>
  <done>
NavBar.ts contains a 3-pill segmented control [3D] [CV] [2D] that dispatches `globe-view-toggle` with `{ mode: '3d' | 'columbus' | '2d' }`, syncs from `globe-mode-changed`, and is visible only on /globe. CSS injected via `<style>` element.
  </done>
</task>

</tasks>

<verification>
1. `frontend/package.json` contains `"cesium"` and `"vite-plugin-static-copy"` but NOT `globe.gl`, `three`, `deck.gl`, `maplibre-gl`, or any `@deck.gl/*` package
2. `frontend/vite.config.ts` defines `CESIUM_BASE_URL`, uses `viteStaticCopy` with 4 targets, has `cesium` manual chunk
3. `frontend/src/components/NavBar.ts` has 3-pill segmented control with mode payload on `globe-view-toggle`
4. No TypeScript errors in NavBar.ts itself (other files may error until Plan 02/03)
</verification>

<success_criteria>
CesiumJS is installed, build infrastructure is configured, old deps are removed, and NavBar is ready for 3-mode scene control. The frontend project can resolve `import { Viewer } from 'cesium'` without error.
</success_criteria>

<output>
After completion, create `.planning/phases/28-cesiumjs-globe-renderer/28-01-SUMMARY.md`
</output>
