---
phase: 26-operational-fixes-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/polymarket/auto_forecaster.py
  - src/db/models.py
  - src/api/schemas/forecast.py
  - src/api/services/forecast_service.py
  - alembic/versions/20260309_011_narrative_summary.py
  - frontend/src/types/api.ts
autonomous: true

must_haves:
  truths:
    - "Polymarket auto-forecaster skips non-binary markets and logs the skip"
    - "Existing non-binary comparisons are marked excluded in the database"
    - "New forecasts have a narrative_summary generated and stored"
    - "ForecastResponse API returns narrative_summary field"
  artifacts:
    - path: "src/polymarket/auto_forecaster.py"
      provides: "is_binary_market filter in candidate loop"
      contains: "is_binary_market"
    - path: "alembic/versions/20260309_011_narrative_summary.py"
      provides: "Alembic migration adding narrative_summary column"
      contains: "narrative_summary"
    - path: "src/api/schemas/forecast.py"
      provides: "narrative_summary field on ForecastResponse"
      contains: "narrative_summary"
    - path: "frontend/src/types/api.ts"
      provides: "narrative_summary field on ForecastResponse interface"
      contains: "narrative_summary"
  key_links:
    - from: "src/polymarket/auto_forecaster.py"
      to: "Gamma API event.markets[].outcomes"
      via: "is_binary_market check in candidate loop"
      pattern: "is_binary_market"
    - from: "src/api/services/forecast_service.py"
      to: "src/db/models.py Prediction.narrative_summary"
      via: "persist_forecast stores narrative"
      pattern: "narrative_summary"
---

<objective>
Backend fixes: Polymarket binary-only filtering, narrative summary column + generation, schema/DTO updates, and poller verification.

Purpose: Fix operational bug where non-binary Polymarket markets (e.g., "Who will be the next X?") produce nonsensical forecasts, and add the narrative_summary data pipeline that Plan 03's ScenarioExplorer root node will consume.
Output: Binary filter active in auto_forecaster, narrative_summary column on Prediction, ForecastResponse schema updated, TypeScript interface updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-operational-fixes-ux-polish/26-CONTEXT.md
@.planning/phases/26-operational-fixes-ux-polish/26-RESEARCH.md
@src/polymarket/auto_forecaster.py
@src/db/models.py
@src/api/schemas/forecast.py
@src/api/services/forecast_service.py
@frontend/src/types/api.ts
@src/scheduler/registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polymarket binary market filter + exclusion script</name>
  <files>
    src/polymarket/auto_forecaster.py
  </files>
  <action>
    1. Add `is_binary_market(event: dict) -> bool` function to `auto_forecaster.py`. The function must check ALL markets in `event.get("markets", [])` -- return True only when every market has exactly `outcomes == ["Yes", "No"]` (case-sensitive, matching Gamma API convention). Return False if markets list is empty.

    2. In the candidate loop (~line 317-336, the `for event in geo_events:` block), add the binary check immediately after the `event_id in existing_event_ids` dedup check. Non-binary events should be logged at INFO level: `"Skipping non-binary market: %s (event=%s)"` with the title truncated to 60 chars and event_id. Then `continue`.

    3. Add a one-time async utility function `exclude_nonbinary_comparisons(session: AsyncSession) -> int` that:
       - Queries all PolymarketComparison rows with status='active'
       - For each, fetches the event from Gamma API via `self._gamma_client` (or direct httpx call to `https://gamma-api.polymarket.com/events/{event_id}`) to check outcomes
       - Sets `status = 'excluded'` for any non-binary ones (String(20) column, no migration needed)
       - Returns count of excluded rows
       - Log each exclusion at INFO level
       This function can be a standalone module-level async function importable for one-time use.

    4. Verify: `polymarket_enabled` defaults to True in settings.py. The Polymarket poller job is already registered conditionally in `registry.py` line 151. The `heavy_baseline_risk` job is registered unconditionally at 3600s interval. Both are already wired -- no code changes needed for enablement. Log a verification note.
  </action>
  <verify>
    - `grep -n "is_binary_market" src/polymarket/auto_forecaster.py` shows the filter function
    - `grep -n "exclude_nonbinary" src/polymarket/auto_forecaster.py` shows the exclusion utility
    - `grep -n "Skipping non-binary" src/polymarket/auto_forecaster.py` shows the log line in the candidate loop
    - `uv run pytest tests/ -v -k "polymarket" --no-header 2>&1 | tail -20` -- existing tests still pass
  </verify>
  <done>
    is_binary_market filter is in the candidate loop. Non-binary markets are skipped with logging. exclude_nonbinary_comparisons utility exists for one-time DB cleanup. Existing Polymarket tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Narrative summary column + generation pipeline + DTO updates</name>
  <files>
    src/db/models.py
    alembic/versions/20260309_011_narrative_summary.py
    src/api/schemas/forecast.py
    src/api/services/forecast_service.py
    frontend/src/types/api.ts
  </files>
  <action>
    1. Add `narrative_summary` column to `Prediction` model in `src/db/models.py`:
       ```python
       narrative_summary: Mapped[Optional[str]] = mapped_column(
           Text, nullable=True, default=None
       )
       ```
       Place it after the `evidence_count` field (line ~80).

    2. Create Alembic migration `alembic/versions/20260309_011_narrative_summary.py`:
       - `op.add_column('predictions', sa.Column('narrative_summary', sa.Text(), nullable=True))`
       - Down: `op.drop_column('predictions', 'narrative_summary')`
       - Follow the naming/revision pattern of existing migrations (check the last migration for revision chain).

    3. Add `narrative_summary: Optional[str] = Field(None, description="LLM-generated narrative summary of the forecast situation")` to `ForecastResponse` in `src/api/schemas/forecast.py`. Place it after `evidence_count`.

    4. Update `persist_forecast()` in `src/api/services/forecast_service.py`:
       - After the Prediction row is constructed but before `session.add()`, generate the narrative via a Gemini call.
       - Use a concise prompt: given question, prediction text, probability, and top 2-3 scenario descriptions, ask for a 2-3 sentence analytical narrative explaining the current situation and key factors.
       - Use the existing `google.genai` SDK pattern already used elsewhere in the codebase (check `src/forecasting/ensemble_predictor.py` or `src/forecasting/llm_forecaster.py` for the import pattern).
       - Set `prediction_row.narrative_summary = narrative_text`.
       - Wrap in try/except -- if narrative generation fails, log warning and leave it None. Never block forecast persistence on narrative failure.
       - This counts as 1 Gemini call against the daily budget.

    5. Update `ForecastResponse` TypeScript interface in `frontend/src/types/api.ts`:
       - Add `narrative_summary: string | null;` after `evidence_count` in the `ForecastResponse` interface.

    6. Update `_to_response()` or equivalent DTO-reconstruction function in `forecast_service.py` to include `narrative_summary` from the Prediction row.
  </action>
  <verify>
    - `grep -n "narrative_summary" src/db/models.py` shows the column
    - `grep -n "narrative_summary" src/api/schemas/forecast.py` shows the Pydantic field
    - `grep -n "narrative_summary" frontend/src/types/api.ts` shows the TS field
    - `ls alembic/versions/20260309_011_narrative_summary.py` confirms migration exists
    - `uv run pytest tests/ -v --no-header -x 2>&1 | tail -20` -- tests pass
  </verify>
  <done>
    Prediction model has nullable narrative_summary column. Alembic migration exists. ForecastResponse Pydantic schema and TypeScript interface both include narrative_summary. persist_forecast generates narrative via Gemini with graceful fallback. DTO reconstruction includes the field.
  </done>
</task>

</tasks>

<verification>
- Binary filter function exists and is called in the candidate loop
- Narrative summary column added with nullable=True
- Alembic migration file exists and is syntactically valid
- ForecastResponse schema includes narrative_summary in both Python and TypeScript
- All existing tests pass
- Poller jobs confirmed as already registered (no code change needed)
</verification>

<success_criteria>
1. `is_binary_market()` correctly filters events where ANY market has non-["Yes","No"] outcomes
2. `exclude_nonbinary_comparisons()` utility ready for one-time execution
3. `narrative_summary` column on Prediction is nullable, migration is reversible
4. Narrative generation in persist_forecast is wrapped in try/except (never blocks persistence)
5. TypeScript ForecastResponse interface has `narrative_summary: string | null`
</success_criteria>

<output>
After completion, create `.planning/phases/26-operational-fixes-ux-polish/26-01-SUMMARY.md`
</output>
