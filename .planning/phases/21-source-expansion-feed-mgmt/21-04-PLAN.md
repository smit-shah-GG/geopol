---
phase: 21-source-expansion-feed-mgmt
plan: 04
type: execute
wave: 3
depends_on: ["21-03"]
files_modified:
  - frontend/src/components/LiveStreamsPanel.ts
  - frontend/src/components/SettingsModal.ts
  - frontend/src/screens/dashboard-screen.ts
  - frontend/src/styles/main.css
autonomous: true

must_haves:
  truths:
    - "LiveStreamsPanel shows YouTube live stream embeds with region filter pills"
    - "LiveStreamsPanel pauses streams after 5 minutes of user inactivity"
    - "User can toggle source categories and individual feeds via SettingsModal"
    - "Source preferences persist in localStorage across sessions"
    - "SettingsModal Sources tab has checkbox grid with search and select all/none"
  artifacts:
    - path: "frontend/src/components/LiveStreamsPanel.ts"
      provides: "YouTube live stream panel with idle detection"
      min_lines: 200
    - path: "frontend/src/components/SettingsModal.ts"
      provides: "Settings modal with Sources tab for feed preferences"
      min_lines: 150
  key_links:
    - from: "frontend/src/components/LiveStreamsPanel.ts"
      to: "YouTube IFrame API"
      via: "Dynamic script injection + YT.Player"
      pattern: "onYouTubeIframeAPIReady|YT\\.Player"
    - from: "frontend/src/components/SettingsModal.ts"
      to: "localStorage"
      via: "geopol-disabled-sources key"
      pattern: "geopol-disabled-sources"
---

<objective>
Create a LiveStreamsPanel with YouTube live stream embeds, region filtering, and idle detection, plus a SettingsModal with a Sources tab for granular user feed preferences persisted in localStorage.

Purpose: Adds live video monitoring capability and user control over which news sources appear in the feed. Both are core to the WM-style news ecosystem experience.
Output: LiveStreamsPanel, SettingsModal with Sources tab, localStorage preference persistence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-expansion-feed-mgmt/21-RESEARCH.md
@.planning/phases/21-source-expansion-feed-mgmt/21-03-SUMMARY.md

@frontend/src/components/Panel.ts
@frontend/src/screens/dashboard-screen.ts
@frontend/src/utils/dom-utils.ts
@frontend/src/styles/main.css

WM reference files (READ for porting patterns):
@/home/kondraki/personal/worldmonitor/src/components/LiveNewsPanel.ts
@/home/kondraki/personal/worldmonitor/src/components/UnifiedSettings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LiveStreamsPanel with YouTube IFrame API and idle detection</name>
  <files>
    frontend/src/components/LiveStreamsPanel.ts
    frontend/src/screens/dashboard-screen.ts
    frontend/src/styles/main.css
  </files>
  <action>
    1. **Create LiveStreamsPanel** (`frontend/src/components/LiveStreamsPanel.ts`):
       - Extends `Panel` base class: `{ id: 'live-streams', title: 'LIVE STREAMS', showCount: false }`.
       - **Channel list**: Static `CHANNELS: LiveChannel[]` array of 15-20 curated geopolitical news channels. Interface: `{ id: string, name: string, youtubeId: string, region: string, language?: string }`.
         Channels: Al Jazeera English, BBC News, CNN, Sky News, France 24 English, DW News, WION, TRT World, NHK World, i24NEWS, Euronews, CNA (Channel News Asia), ABC News Live, MSNBC, Reuters TV. Add YouTube video IDs for their live streams (these are well-known public IDs — use the channel's live stream URL pattern).
       - **Region pills**: Row of filter buttons: All, Americas, Europe, Middle East, Asia-Pacific, Africa. Filter channels by region.
       - **YouTube IFrame API loading**: Load the API script dynamically on first mount (not at page load). Set `window.onYouTubeIframeAPIReady` BEFORE injecting the `<script>` tag. Queue player creation until the callback fires. If API already loaded, create players immediately.
       - **Player grid**: 2-column grid of YouTube players. Each player: 16:9 aspect ratio iframe, channel name label below, live indicator dot, mute/unmute button. Only create `YT.Player` for visible channels (filtered by region). Destroy and recreate players on filter change.
       - **Idle detection**: Track `mousemove`, `keypress`, `click`, `scroll` events on document. After 5 minutes of no activity, pause all active players (`player.pauseVideo()`). On resume (any interaction), play the first visible player (`player.playVideo()`).
       - **Mute controls**: All players start muted (`playerVars: { mute: 1 }`). Per-player mute toggle. Only one player can be unmuted at a time (unmuting one mutes all others).
       - **Always visible**: Panel is not collapsible. No minimize/maximize behavior.
       - `destroy()`: Destroy all YT.Player instances, remove idle detection listeners.

    2. **Wire into dashboard-screen.ts**:
       - Import LiveStreamsPanel.
       - Instantiate and mount in Col 1, below NewsFeedPanel (append after newsFeedPanel element).
       - Register in ctx.panels as 'live-streams'.
       - No refresh scheduler needed — YouTube handles its own stream state.

    3. **CSS**: Add LiveStreamsPanel styles. 2-column player grid, 16:9 aspect ratio containers, region pills matching NewsFeedPanel pill style, live indicator (pulsing red dot), mute button overlay.

    PITFALL: YouTube IFrame API loading race — MUST set `window.onYouTubeIframeAPIReady` before injecting the script tag. Use a Promise wrapper: `loadYouTubeAPI(): Promise<void>` that resolves when the callback fires or immediately if already loaded.
  </action>
  <verify>
    `cd frontend && npm run build` succeeds. LiveStreamsPanel imported in dashboard-screen.ts. YouTube API loader sets callback before script injection. Idle detection timeout is 5 minutes (300000ms). Players start muted.
  </verify>
  <done>LiveStreamsPanel renders 15-20 curated YouTube live streams in a 2-column grid with region pills, idle detection (5 min), mute controls, and live indicators. Mounted in Col 1 below NewsFeedPanel.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsModal with Sources tab</name>
  <files>
    frontend/src/components/SettingsModal.ts
    frontend/src/screens/dashboard-screen.ts
    frontend/src/styles/main.css
  </files>
  <action>
    1. **Create SettingsModal** (`frontend/src/components/SettingsModal.ts`):
       - Overlay modal triggered by a settings gear icon (add to NavBar or dashboard header).
       - **Tab structure**: Start with single "Sources" tab. Architecture supports adding more tabs later.
       - **Sources tab** (port WM's UnifiedSettings Sources section):
         - **Category toggles**: Row of toggle pills for each category (Wire, Mainstream, Defense, Think Tank, Regional, Crisis, Finance, Energy, Government, Intel). Toggling a category enables/disables ALL feeds in that category.
         - **Feed search**: Text input that filters the feed list by name.
         - **Feed checkbox grid**: 2-3 column grid of individual feed checkboxes. Each shows: feed name, tier badge (T1/T2), enabled checkbox. Feeds grouped by category with section headers.
         - **Select All / Select None** buttons.
         - **Disabled sources counter**: "N sources hidden" label showing how many feeds are currently disabled.
       - **localStorage persistence**:
         - Key: `geopol-disabled-sources`.
         - Value: JSON array of disabled source feed names (e.g., `["Xinhua", "TASS"]`).
         - `getDisabledSources(): Set<string>` reads from localStorage.
         - `setDisabledSources(sources: Set<string>)` writes to localStorage.
         - On change, dispatch `geopol:sources-changed` CustomEvent so NewsFeedPanel can re-filter.
       - **Feed data**: Use a static `ALL_SOURCE_NAMES` array derived from the same source tier map in NewsFeedPanel. Include name, tier, category for each.
       - **Modal behavior**: ESC to close, click backdrop to close, smooth fade in/out animation.
       - `destroy()`: Remove modal from DOM, remove event listeners.

    2. **Wire settings trigger**:
       - Add a small gear icon button in the dashboard header area (near SearchBar or in NavBar).
       - Clicking opens the SettingsModal.

    3. **CSS**: Modal overlay, tab bar, checkbox grid, category pills, search input. Dark theme consistent. Backdrop blur.

    NOTE: SettingsModal dispatches `geopol:sources-changed` CustomEvent on source toggle. The NewsFeedPanel listener for this event is wired in Plan 05 (Task 2), which adds source filtering to NewsFeedPanel. Until Plan 05 executes, the event dispatches but has no consumer — this is intentional and harmless.
  </action>
  <verify>
    `cd frontend && npm run build` succeeds. SettingsModal can be instantiated. localStorage read/write works. `geopol:sources-changed` CustomEvent is dispatched on source toggle. Category toggles affect individual feed checkboxes.
  </verify>
  <done>SettingsModal with Sources tab provides category-level and feed-level toggle controls. Preferences persisted in localStorage as disabled source set. CustomEvent dispatched on change. NewsFeedPanel listener for sources-changed is wired in Plan 05.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds
2. LiveStreamsPanel mounts in Col 1 below NewsFeedPanel
3. YouTube API loader uses Promise wrapper with pre-set callback
4. SettingsModal opens/closes with ESC and backdrop click
5. localStorage read/write for disabled sources works
6. `geopol:sources-changed` event dispatched on toggle
</verification>

<success_criteria>
- LiveStreamsPanel shows 15-20 curated YouTube streams in 2-column grid
- Region pills filter visible channels
- Idle detection pauses all players after 5 min inactivity
- Only one player unmuted at a time
- SettingsModal Sources tab: category toggles, feed checkboxes, search, select all/none
- Disabled sources persisted in localStorage, event dispatched on change
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-expansion-feed-mgmt/21-04-SUMMARY.md`
</output>
