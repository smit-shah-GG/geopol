---
phase: 21-source-expansion-feed-mgmt
plan: 03
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - frontend/src/components/WindowedList.ts
  - frontend/src/components/NewsFeedPanel.ts
  - frontend/src/components/BreakingNewsBanner.ts
  - frontend/src/screens/dashboard-screen.ts
  - frontend/src/styles/main.css
  - frontend/src/app/panel-layout.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard columns are 25-30-30-15 with NewsFeedPanel in Col 1"
    - "EventTimelinePanel is deleted from dashboard"
    - "SourcesPanel is removed from dashboard Col 3"
    - "RiskIndexPanel moved to Col 4"
    - "NewsFeedPanel shows clustered articles from ChromaDB with source count badges"
    - "NewsFeedPanel uses virtual scrolling for performance"
    - "NewsFeedPanel has category filter pills"
    - "BreakingNewsBanner appears full-width above columns on high-severity events"
  artifacts:
    - path: "frontend/src/components/NewsFeedPanel.ts"
      provides: "Clustered news feed with virtual scrolling"
      min_lines: 200
    - path: "frontend/src/components/WindowedList.ts"
      provides: "WM-ported virtual scrolling component"
      min_lines: 100
    - path: "frontend/src/components/BreakingNewsBanner.ts"
      provides: "Full-width alert banner with auto-dismiss"
      min_lines: 100
  key_links:
    - from: "frontend/src/components/NewsFeedPanel.ts"
      to: "/api/v1/articles?sort=recent"
      via: "forecastClient fetch in refresh()"
      pattern: "articles.*sort.*recent"
    - from: "frontend/src/screens/dashboard-screen.ts"
      to: "frontend/src/components/NewsFeedPanel.ts"
      via: "import and mount in Col 1"
      pattern: "NewsFeedPanel"
    - from: "frontend/src/components/BreakingNewsBanner.ts"
      to: "geopol:breaking-news"
      via: "CustomEvent listener"
      pattern: "geopol:breaking-news"
---

<objective>
Reorganize the dashboard layout from 15-35-30-20 to 25-30-30-15 columns, replace EventTimelinePanel with a WM-style NewsFeedPanel (article clustering, virtual scrolling, category pills, source tier badges), and add a BreakingNewsBanner for high-severity event alerts.

Purpose: Transforms the dashboard from sparse event listing to a rich news ecosystem. Users see clustered, credibility-tagged news with virtual scrolling performance.
Output: New dashboard layout, NewsFeedPanel, WindowedList utility, BreakingNewsBanner.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-expansion-feed-mgmt/21-RESEARCH.md
@.planning/phases/21-source-expansion-feed-mgmt/21-02-SUMMARY.md

@frontend/src/screens/dashboard-screen.ts
@frontend/src/components/Panel.ts
@frontend/src/components/EventTimelinePanel.ts
@frontend/src/styles/main.css
@frontend/src/app/panel-layout.ts
@frontend/src/services/forecast-client.ts
@frontend/src/types/api.ts
@frontend/src/utils/dom-utils.ts

WM reference files (READ for porting patterns):
@/home/kondraki/personal/worldmonitor/src/components/NewsPanel.ts
@/home/kondraki/personal/worldmonitor/src/components/VirtualList.ts
@/home/kondraki/personal/worldmonitor/src/components/BreakingNewsBanner.ts
@/home/kondraki/personal/worldmonitor/src/config/feeds.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard layout reorganization + WindowedList port</name>
  <files>
    frontend/src/styles/main.css
    frontend/src/app/panel-layout.ts
    frontend/src/screens/dashboard-screen.ts
    frontend/src/components/WindowedList.ts
  </files>
  <action>
    1. **CSS column widths**: In `frontend/src/styles/main.css`, find the dashboard column width definitions (lines ~253-256 per research). Change from `15/35/30/20` to `25/30/30/15`. Ensure min-width values are adjusted proportionally (Col 1 gets wider, Col 4 gets narrower).

    2. **panel-layout.ts**: Update the comment header describing column assignments to reflect new layout. No structural code changes needed — the layout is CSS-driven.

    3. **dashboard-screen.ts panel rewiring**:
       - Remove `EventTimelinePanel` import and instantiation.
       - Remove `SourcesPanel` import and instantiation.
       - Move `RiskIndexPanel` from Col 1 to Col 4 (before healthPanel).
       - Col 1 will mount NewsFeedPanel (created in Task 2) — for now, leave Col 1 ready for it.
       - Remove the `events` and `sources` entries from `scheduler.registerAll()`.
       - Remove from `ctx.panels` registration: `event-timeline`, `sources`.
       - Add BreakingNewsBanner instantiation (not a Panel, standalone — attach to body, see Task 3).
       - Do NOT delete EventTimelinePanel.ts or SourcesPanel.ts files yet — they may be referenced elsewhere. Just remove from dashboard mounting.

    4. **WindowedList port**: Create `frontend/src/components/WindowedList.ts` by porting WM's `WindowedList<T>` class (from `/home/kondraki/personal/worldmonitor/src/components/VirtualList.ts`, lines ~244-406).
       - Constructor: `(options: { container: HTMLElement; chunkSize?: number; bufferChunks?: number }, renderItem: (item: T, index: number) => string, onRendered?: () => void)`
       - `setItems(items: T[]): void` — full replace + re-render visible chunks.
       - `refresh(): void` — re-render visible chunks.
       - `destroy(): void` — cleanup scroll listeners, remove sentinel.
       - Use `requestAnimationFrame` for scroll handling (WM pattern).
       - Chunk-based rendering: only render chunks within viewport + buffer. Sentinel element for measuring total height.
       - Adapt to geopol conventions: use geopol's `h()` helper if beneficial, but the core algorithm is pure DOM manipulation — keep WM's approach.

    PITFALL: BreakingNewsBanner uses fixed positioning above the dashboard. Add `--breaking-alert-offset` CSS custom property and `has-breaking-alert` body class. Dashboard columns need a CSS rule that adds `padding-top` when `.has-breaking-alert` is present on body.
  </action>
  <verify>
    `cd frontend && npm run build` succeeds. No import errors. Dashboard columns render at 25/30/30/15 widths. EventTimelinePanel and SourcesPanel no longer appear in the dashboard mount code.
  </verify>
  <done>Dashboard layout is 25-30-30-15. RiskIndexPanel in Col 4. EventTimelinePanel and SourcesPanel removed from dashboard. WindowedList utility ported from WM. CSS supports breaking-alert offset.</done>
</task>

<task type="auto">
  <name>Task 2: NewsFeedPanel with clustering and source badges</name>
  <files>
    frontend/src/components/NewsFeedPanel.ts
    frontend/src/screens/dashboard-screen.ts
    frontend/src/services/forecast-client.ts
    frontend/src/types/api.ts
  </files>
  <action>
    1. **Add ArticleDTO to types** if not already present (check `frontend/src/types/api.ts`). Ensure it has: chunk_id, title, url, source_feed, country_iso, published_at, snippet, relevance_score. Add `source_tier?: number` and `propaganda_risk?: string` fields for frontend enrichment.

    2. **Add forecastClient method**: `getRecentArticles(limit?: number): Promise<ArticleDTO[]>` that calls `GET /api/v1/articles?sort=recent&limit={limit || 100}`. Parse the PaginatedResponse and return `.items`.

    3. **Create NewsFeedPanel** (`frontend/src/components/NewsFeedPanel.ts`):
       - Extends `Panel` base class: `{ id: 'news-feed', title: 'NEWS FEED', showCount: true }`.
       - **Category pills**: Row of filter buttons at top of panel: All, Wire, Mainstream, Defense, Think Tank, Regional, Crisis, Finance. Active pill has `.active` class. Clicking sets `this.categoryFilter` and re-renders.
       - **Article clustering**: `clusterArticles(articles: ArticleDTO[])` function:
         - Normalize each title to lowercase word tokens (strip punctuation, common stop words).
         - Jaccard similarity on token sets. Threshold: 0.5 (same story).
         - Group into clusters. Each cluster: `{ primaryTitle, primaryUrl, primarySource, sourceCount, topSources: {name, tier}[], lastUpdated: Date, articles: ArticleDTO[] }`.
         - Sort clusters by most recent article timestamp.
       - **Source tier enrichment**: Port a subset of WM's source tier data. Map source_feed names to tiers: Tier 1 (wire: Reuters, AP, AFP, Bloomberg), Tier 2 (BBC, Guardian, CNN, etc.), Tier 3 (think tanks), Tier 4 (regional). Use a static `SOURCE_TIERS: Record<string, number>` map. Also port WM's `PROPAGANDA_RISK` map (~20 entries).
       - **Rendering**: Use WindowedList for the cluster list. Each cluster renders as:
         - Primary headline (linked to primaryUrl).
         - Source badge with tier color (T1=gold, T2=blue, T3=green, T4=gray).
         - "N sources" badge if sourceCount > 1, with tooltip showing source names.
         - Propaganda risk indicator if source has medium/high risk (small icon/label).
         - Relative timestamp.
         - Snippet (first 150 chars).
       - **Refresh**: Called from RefreshScheduler. Fetches recent articles, clusters, filters by category, renders via WindowedList.
       - **Category filter**: Filters clusters where any article's source_feed matches a feed in the selected category. "All" shows everything.

    4. **Wire into dashboard-screen.ts**:
       - Import NewsFeedPanel.
       - Instantiate and mount in Col 1.
       - Register in ctx.panels as 'news-feed'.
       - Add to scheduler.registerAll: name 'news-feed', fn calls newsFeedPanel.refresh(), intervalMs 60_000.

    STYLE: Add CSS for NewsFeedPanel in main.css or a new news-feed.css import. Category pills should match the existing geopol dark theme. Cluster cards should have subtle borders, hover states.
  </action>
  <verify>
    `cd frontend && npm run build` succeeds. NewsFeedPanel is imported and mounted in dashboard-screen.ts Col 1. Refresh cycle registered. Category pills render. Clustering function handles empty arrays without crash.
  </verify>
  <done>NewsFeedPanel renders clustered articles with source tier badges, propaganda risk indicators, category filter pills, and virtual scrolling. Mounted in Col 1 of the reorganized dashboard. Refreshes every 60s from /api/v1/articles?sort=recent.</done>
</task>

<task type="auto">
  <name>Task 3: BreakingNewsBanner</name>
  <files>
    frontend/src/components/BreakingNewsBanner.ts
    frontend/src/screens/dashboard-screen.ts
  </files>
  <action>
    1. **Create BreakingNewsBanner** (`frontend/src/components/BreakingNewsBanner.ts`):
       - NOT a Panel subclass — standalone class appended to `document.body`.
       - Listens for `geopol:breaking-news` CustomEvent on `document`.
       - Event detail: `{ id: string, headline: string, source: string, threatLevel: 'critical' | 'high', timestamp: Date }`.
       - **Max concurrent**: 3 alerts on screen. If 4th arrives, dismiss the oldest.
       - **Auto-dismiss**: critical=60s, high=30s. Uses `setTimeout`. Pause timer on `document.visibilitychange` (hidden) and resume on visible (WM pattern).
       - **Severity colors**: critical = red background (#dc2626), high = amber (#d97706). Both with white text.
       - **Layout**: Fixed position at top of viewport. Each alert is a horizontal bar with: severity icon, headline text, source name, dismiss X button. Stack vertically (newest on top).
       - **Body class**: Toggle `has-breaking-alert` on body when alerts are visible. Set `--breaking-alert-offset` CSS custom property to total height of visible alerts.
       - **Sound**: Optional. Use WM's base64 WAV approach — a tiny inline notification sound. Play with 5-min cooldown between sounds. Respect `localStorage.getItem('geopol-alert-sound')` preference (default: off).
       - **Click**: Clicking the headline text dispatches a navigation event (for future wiring to forecast detail).
       - `destroy()`: Remove all alerts, remove event listener, remove body class.

    2. **Trigger integration in dashboard-screen.ts**:
       - After each forecast refresh, check for probability spikes: if any forecast probability changed by >0.15 since last refresh, dispatch `geopol:breaking-news` event.
       - After each event refresh (if event data is still fetched), check for extreme Goldstein scale (|value| >= 8.0) and dispatch.
       - Instantiate BreakingNewsBanner in `mountDashboard()`, destroy in `unmountDashboard()`.

    3. **CSS**: Add banner styles in main.css. Fixed positioning, z-index above everything, slide-down animation on appear, slide-up on dismiss.

    PITFALL: Banner must not permanently shift dashboard content. Use CSS transitions on `--breaking-alert-offset` so the dashboard padding adjusts smoothly.
  </action>
  <verify>
    `cd frontend && npm run build` succeeds. BreakingNewsBanner listens for CustomEvent. Dispatching a test event creates a visible alert. Auto-dismiss timer works. Max 3 concurrent.
  </verify>
  <done>BreakingNewsBanner renders full-width alerts above dashboard on high-severity events. Auto-dismiss with visibility-aware timers, max 3 concurrent, severity colors, optional sound, click-through navigation.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds with zero errors
2. Dashboard renders 4 columns at 25/30/30/15 widths
3. NewsFeedPanel visible in Col 1 with category pills
4. BreakingNewsBanner container attached to body
5. EventTimelinePanel and SourcesPanel not mounted in dashboard
6. RiskIndexPanel in Col 4
7. WindowedList imported and used by NewsFeedPanel
</verification>

<success_criteria>
- Dashboard layout is 25-30-30-15 with correct panel placement
- EventTimelinePanel deleted from dashboard, SourcesPanel removed
- NewsFeedPanel shows clustered articles with source tiers, propaganda risk, category filtering
- WindowedList provides virtual scrolling for the news feed
- BreakingNewsBanner fires on extreme events, auto-dismisses, stacks up to 3
- All panels refresh on scheduler intervals
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-expansion-feed-mgmt/21-03-SUMMARY.md`
</output>
