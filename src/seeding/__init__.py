"""Global seeding package: baseline risk computation for all countries.

Provides FIPS-to-ISO conversion, population lookup, and composite
risk score computation from GDELT + ACLED + travel advisory inputs.
"""
